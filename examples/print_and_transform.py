# Monty forwards print output and returns the last expression.

items = ["monty", "pera", "runtime"]
lengths = {item: len(item) for item in items}

print("items:", items)
print("lengths:", lengths)

lengths
