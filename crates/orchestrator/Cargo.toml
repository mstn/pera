[package]
name = "pera-orchestrator"
version.workspace = true
edition.workspace = true

[dependencies]
async-trait = "0.1"
pera-core = { path = "../core" }

[dev-dependencies]
tokio = { version = "1.48.0", features = ["macros", "rt-multi-thread"] }

[lib]
path = "src/lib.rs"
