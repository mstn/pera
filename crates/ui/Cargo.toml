[package]
name = "pera-ui"
version.workspace = true
edition.workspace = true

[dependencies]
pera-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
path = "src/lib.rs"
